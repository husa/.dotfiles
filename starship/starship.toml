"$schema" = 'https://starship.rs/config-schema.json'

format = """\
$shell\
$sudo\
$all\
$fill\
$cmd_duration\
$status\
$time\
$line_break\
[❯](yellow ) """
right_format = "$character"


[fill]
symbol = ' '

[aws]
disabled = true

[time]
disabled = false
format = '[$time]($style) '
style = 'yellow italic'

[shell]
disabled = false
fish_indicator = "🐟"
zsh_indicator = '\$'

[character]
success_symbol = "[[](green bg:none) INSERT ](bold black bg:prev_fg)"
error_symbol = "[[](green bg:none) INSERT ](bold black bg:prev_fg)"
vimcmd_symbol = "[[](blue bg:none) NORMAL ](bold black bg:prev_fg)"
vimcmd_visual_symbol = "[[](purple bg:none) VISUAL ](bold black bg:prev_fg)"
vimcmd_replace_one_symbol = "[[](cyan bg:none) REPLACE ONE ](bold black bg:prev_fg)"
vimcmd_replace_symbol = "[[](cyan bg:none) REPLACE ](bold black bg:prev_fg)"


[status]
disabled = false

[sudo]
disabled = false

# No Runtime Versions Preset
[bun]
format = "via [$symbol]($style)"

[buf]
format = "via [$symbol]($style)"

[cmake]
format = "via [$symbol]($style)"

[cobol]
format = "via [$symbol]($style)"

[crystal]
format = "via [$symbol]($style)"

[daml]
format = "via [$symbol]($style)"

[dart]
format = "via [$symbol]($style)"

[deno]
format = "via [$symbol]($style)"

[dotnet]
format = "[$symbol(🎯 $tfm )]($style)"

[elixir]
format = 'via [$symbol]($style)'

[elm]
format = 'via [$symbol]($style)'

[erlang]
format = 'via [$symbol]($style)'

[fennel]
format = 'via [$symbol]($style)'

[gleam]
format = 'via [$symbol]($style)'

[golang]
format = 'via [$symbol]($style)'

[gradle]
format = 'via [$symbol]($style)'

[haxe]
format = 'via [$symbol]($style)'

[helm]
format = 'via [$symbol]($style)'

[java]
format = 'via [$symbol]($style)'

[julia]
format = 'via [$symbol]($style)'

[kotlin]
format = 'via [$symbol]($style)'

[lua]
format = 'via [$symbol]($style)'

[meson]
format = 'via [$symbol]($style)'

[nim]
format = 'via [$symbol]($style)'

[nodejs]
format = 'via [$symbol]($style)'

[ocaml]
format = 'via [$symbol(\($switch_indicator$switch_name\) )]($style)'

[opa]
format = 'via [$symbol]($style)'

[perl]
format = 'via [$symbol]($style)'

[php]
format = 'via [$symbol]($style)'

[pulumi]
format = 'via [$symbol$stack]($style)'

[purescript]
format = 'via [$symbol]($style)'

[python]
format = 'via [$symbol]($style)'

[quarto]
format = 'via [$symbol]($style)'

[raku]
format = 'via [$symbol]($style)'

[red]
format = 'via [$symbol]($style)'

[rlang]
format = 'via [$symbol]($style)'

[ruby]
format = 'via [$symbol]($style)'

[rust]
format = 'via [$symbol]($style)'

[solidity]
format = 'via [$symbol]($style)'

[typst]
format = 'via [$symbol]($style)'

[swift]
format = 'via [$symbol]($style)'

[vagrant]
format = 'via [$symbol]($style)'

[vlang]
format = 'via [$symbol]($style)'

[zig]
format = 'via [$symbol]($style)'
